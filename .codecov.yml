ignore:
   - "Pods"
   - "Tests"

coverage:
  status:
    patch:
      default:
        target: 80%
        threshold: 5%
        base: auto 
        if_ci_failed: error
    project:
      default:
        target: 50%
        threshold: 5%
        base: auto
        if_ci_failed: error